# 数学课程与教学设计 Harness：交付规格

Status: ready-for-agent

日期：2026-09-15。实施状态：尚未开始。本规格承接 [项目目标与原则](../../README.md)、[架构结论](../skills-harness/assets/architecture-conclusion.md) 与 [验证路线](../skills-harness/assets/validation-and-release-route.md)。任务顺序和依赖见 [交付路线与实现票](README.md)。`ready-for-agent` 表示要求已可用于实施，不表示教学质量或生产能力已通过。

实现或评审每张票前还需读 [用户交互原型基准](prototype-baseline.md)。用户要求实施前重新确认面向课程负责人／教师的具体交互；当前为待确认候选。原型表达外部消费者的交互语义，不承担 Web 产品建设或真实教学质量验证。

## Problem Statement

课程负责人需要设计自己的数学课程体系，并将年级目标、学习进程和设计理由落实为可使用、可修订的课时、任务与评测。教师需要围绕这些真实内容进行适配、备课和理解度检查。学校条件、学习证据和个体 Learner Model 应实际影响设计，而不能只出现在提示词或材料开头。

原 Skills 提供重要的教学流程、模型使用和检查经验，但 Web 后端还需要承载实际 Context、知识查询、材料生成、真实人类回应、长任务恢复、版本和执行证据。本仓库交付这组教学执行能力；完整学校产品、学习证据采集、Learner Model 构建和最终发布由外部系统负责。

现有原型分别验证了部分真实生成与有限持久运行，尚未将真实 Gemini、原生模型循环、完整教学流程和服务恢复贯通。连续三课也不能证明完整单元能力，文件存在和模型自评不能证明教学质量。

## Solution

提供 Python 后端 Harness，采用 LangChain 模型／工具循环、LangGraph 必要的持久执行边界、Agent Server 任务承载和 LangSmith 观察与评测。外部产品用少量任务级操作发起工作、展示实际结果、提交自然语言回应和修改；教学执行过程由模型在适用规则与工具下完成。

完整交付范围是下列五项数学能力。第一条贯通路径选择八年级 CCSS 全年蓝图与其中的线性函数单元，先落实连续课时，再完成整个单元；这是实现顺序和首批验证范围，不把项目缩减为教案生成器。

工程首步仅用一个有明确范围的真实课程设计任务贯通模型、知识、任务与内容契约，再分别交付完整全年、一课材料及其连续交接。任务规模和依赖的复核见 [逐票分析](ticket-sizing-review.md)。它改变实施颗粒度，不降低首条全年到课时链或五项能力的验收范围；远端工作包需依据真实产出收口后才转为可执行票。

| 能力 | 输入与实际工作 | 交付与自身完成条件 |
| --- | --- | --- |
| 课程体系设计 | 真实 CCSS 范围、学校条件及适用学习上下文；设计 Grade／Unit／Section／Lesson 的目标分配、学习进程、Narrative、任务与证据，允许下层试做反馈上层 | 按请求范围交付可检查的设计与材料、覆盖和依赖说明；全年蓝图可以单独交付，但不能声称已有全年课时材料；完整单元必须包含全部课时与必要材料 |
| 课时与材料设计 | 使用实际课程位置与版本，或调用方明确的独立课时目标；设计任务、试做、形成草稿、师生材料及检查 | 保留已采用草稿的实际任务，生成并检查所需材料；支持直接生成、先看草稿、仅修改、修改后直接生成；不以教师未回应满意邀请阻止已完成交付 |
| 教学适配 | 从实际原课及相关学习证据出发，保持明确的数学目标，调整入口、支持、支架和拓展 | 交付协调的教师说明与学生材料；正常协作记录对当前整套请求产物的接受；仅部分接受不冒充整套接受，明确只生成供后审、停止或退出分别结束 |
| 教师备课 | 使用真实原课，模型先试做关键任务，围绕具体难点与教师进行有目的的讨论 | 正常路径先取得教师自己的 near-miss 分析／尝试，再反馈并形成便签；先问概念、请求示范、拒答、时间很短或结束按各自规则处理，不能伪造教师贡献 |
| 理解度检查 | 使用实际 CCSS、Learning Components 与前后联系明确焦点；设计能区分理解和困难的任务、答案与教学回应 | 采用当前有效焦点后生成实际学生页与教师指南；对实际文件分别验证可辩护答案和错误推理可达性，再交付；不要求先有一份教案 |

上游详细流程继续作为四项能力的设计和比较起点。其固定阶段、文件数量、课时数量及 IM 专属结构不统一强加到所有任务；取舍须说明原教学目的、替代方法和验证依据。

## User Stories

1. 作为课程负责人，我希望系统使用完整、版本明确的本年级 CCSS 内容与实践标准，以免只围绕一个热门单元规划全年。
2. 作为课程负责人，我希望设计自己的单元组织与位置，以便依据教学目标和实际条件形成课程，而不复制 IM 的目录。
3. 作为课程负责人，我希望每层 Narrative 解释学习如何推进、为什么这样安排，以便审查设计理由而不只阅读摘要。
4. 作为课程负责人，我希望能够追查目标对应的学习机会和证据，以便发现漏教、未练就考及无目的重复。
5. 作为课程负责人，我希望在关键任务试做暴露问题时修订上层安排，以免漂亮的蓝图掩盖无法实施的课堂任务。
6. 作为课程负责人，我希望完整单元包含所有课时和必要材料，以便知道哪些已经可审阅、哪些仍只有计划。
7. 作为课程负责人，我希望压缩课时后重新核对目标、练习、时间及后续依赖，以免仅修改课时数。
8. 作为教师，我希望教案包含真实题目、解法、表征、讨论与学习检查，以便在课堂中实际使用。
9. 作为教师，我希望已有有效的目标、条件和生成授权直接被使用，以免反复回答相同问题。
10. 作为教师，我希望先审草稿时看到真实任务，以便提出有对象的修改。
11. 作为教师，我希望“只改草稿”和“改好直接生成”产生不同的后续动作，以便保持我的实际意图。
12. 作为教师，我希望课时材料延续已采用的任务和条件，以免成套输出时换成另一套内容。
13. 作为教师，我希望学生页、教师答案、图形与可编辑源保持一致，以便修订后不会使用旧答案或坏图。
14. 作为教师，我希望课堂时间估算包含学生工作、讨论、转换及资源准备，以便识别实际不可实施的安排。
15. 作为教师，我希望必要的函数图能够清楚表达数学信息，以便学生无需猜测坐标、单位或标签。
16. 作为教师，我希望适配建立在真实原课和相关证据上，以便支持变化有据可查。
17. 作为教师，我希望获得支持的学生仍进行目标数学思考，以免适配变成低水平替代任务或直接泄露答案。
18. 作为教师，我希望持续性支持与可逐渐撤除的支架分别处理，以免系统自动撤去学生需要的支持。
19. 作为教师，我希望班级或个人适配保持自己的范围，以免意外改动公共课程基线。
20. 作为教师，我希望系统区分对部分材料的接受与整套接受，以便准确表达尚需修改的部分。
21. 作为教师，我希望备课围绕本课关键任务和真实难点展开，以免只收到泛泛教法建议。
22. 作为教师，我希望自己先分析看似正确但有关键缺口的回应，以便练习识别学生思考，而不是阅读系统事先给出的答案。
23. 作为教师，我希望实际贡献影响后续讨论和便签，以便备课过程有意义。
24. 作为教师，我希望先问概念、要求示范或结束讨论时得到相应处理，以便系统尊重当下需要而不机械追问。
25. 作为教师，我希望能仅从标准和教学目标发起理解度检查，以便不必先生成整份教案。
26. 作为教师，我希望检查题不仅判断答对，还能揭示错误方法及部分理解，以便选择具体后续教学。
27. 作为教师，我希望答案、干扰项和解释均来自实际题目验证，以免根据无法产生该作答的“误区”作判断。
28. 作为学生，我希望只看到适合我的任务、图形、说明和作答空间，以免材料泄露答案、教师分析或能力标签。
29. 作为产品后端开发者，我希望用任务级接口传入真实上下文并接收结果，以便 Web 用户不依赖交互式 Agent 客户端。
30. 作为产品后端开发者，我希望取得可直接展示的待答内容及其版本，以便无需让教师理解内部场景表或状态机。
31. 作为产品后端开发者，我希望网络重送同一事件不会重复创建任务或采纳回应，以便可靠地恢复请求。
32. 作为产品后端开发者，我希望新修改意图不会因旧请求已答而丢失，以便保留用户改变决定的权利。
33. 作为产品后端开发者，我希望重连后查询到权威快照，以免用丢失或乱序的流事件拼出错误状态。
34. 作为产品后端开发者，我希望取消后迟到的模型结果不能提交，以免已经取消的工作覆盖有效版本。
35. 作为产品后端开发者，我希望跨进程和部署恢复保持原任务行为或明确要求迁移，以免新代码默默改变等待中的教学流程。
36. 作为审核者，我希望每份交付关联来源、实际输入、人类决定、检查与修订版本，以便核对归因及尚未解决的问题。
37. 作为审核者，我希望不同检查能说明证据和适用范围，以免把程序通过、模型判断或教师满意当成同一种质量保证。
38. 作为维护者，我希望使用相同案例比较改动前后的教学质量，以便知道新机制是否比原 Skills 更有效。
39. 作为维护者，我希望冻结完整原创单元后再匿名比较 IM，以便避免把参考答案带进原创设计。
40. 作为维护者，我希望持续记录不同任务的质量和实际消耗，以便以后有依据地优化，而不先删减必要检查。
41. 作为学校系统负责人，我希望任务和材料遵守可信身份、权限及数据范围，以便不同学校、班级和受众的信息保持隔离。
42. 作为学校系统负责人，我希望观察服务故障不破坏教学任务，以便运维工具不可用时仍可取得已保存结果。

## Implementation Decisions

### 1. 实现范围与教学资源

五项能力分别拥有适用规则、Context、工具、HITL 和完成条件，共享运行承载。初期仅启用已经实现并验证的能力和格式；未实现请求明确返回不支持，不转成另一项能力并冒称完成。数学先行，首批深度验证八年级线性函数及相关先备；其他数学主题的通用入口不代表已验证全部 K–12 数学。

每项能力使用版本化的规则资源，标明上游来源、采用／调整／不适用及理由。教案创建继承已有全部 39 条 rubric 取用映射；适配、备课与 CFU 在各自实现票内逐条落实自己的规则，不能套用教案 rubric 代替。框架调用者实际装配资源并记录清单，不能只把一个开发文档路径交给模型。

既有适配数学规则允许某些数值变化，而共享材料规范强调共同任务。首批实现先完整支持保留核心任务、目标与情境的支持和拓展；涉及核心题目变体的请求必须显式识别、保留与原题的关系并独立验算，不能让共同内容引用覆盖变体。是否已有对应实现要在能力范围中如实返回。这是有范围的实现顺序，不把“永不允许变体”写为教学原则。

### 2. Context 与知识

Context 包含本次适用规则、任务及授权范围、学校条件、相关 Learner Model／学习证据、实际课程和产物版本、真实决定、已完成动作与待修问题。规则和输入有来源及版本；超出容量时按任务拆分和按需读取，不能静默截断关键要求。

全年蓝图先取得本年级完整 CCSS 内容与数学实践视图，按需深入知识节点；单元和课时取得上层目标分配、Narrative、入口／出口、时间与前后依赖，并保留实际任务交接。允许模型使用内置知识进行数学推理、教学创作和合理设计推断；标准原文、图关系、学校事实和学生状态必须有相应真实输入，不由模型补造。

本地 LC 只读接入优先使用 `browse` 能力。完整遍历必要的分页与层级，读取实际 Learning Components 和相关前后关系；“注册了工具但未使用”不计为已取得依据。严格遵守知识使用约定：`supports` 从 LC 指向标准；`buildsTowards` 区分前驱和后继；`relatesTo` 不当先修；层级、组成、课程依赖和对齐角色不互换。保留实体身份、框架身份、版本、来源、原关系方向及用途。查询的完整、部分、空集、排除与失败分别记录。

LVN 研究因素和策略可在适用时补充，但不是所有任务的必需数据源，也不代表某学生的实测状态。已完成的数据本地化和 API 服务由外部提供。

原创运行只获得允许的标准、知识、任务条件和筛选后的通用教学原则；通过工具权限、输入清单和执行隔离排除对应 IM 课程结构、Narrative、活动与答案，包括混在研究或图返回值里的内容。完整原创单元冻结后才向比较环境开放对应参考。不能承诺消除模型预训练记忆。

### 3. 模型循环与工具

首条真实路径将 Gemini `gemini-3.8-flash` 与 LangChain 原生 agent 循环、实际工具和持久任务结合；固定模型探针与手写循环的单独成功不能替代这一验收。模型及依赖版本在构建和执行记录中固定。Claude、OpenAI 与开源模型通过原生适配分别验证，不先建设自动选模、多模型投票或便宜模型回退。

默认采用单个有充分资源的模型／工具循环；Grade、Unit、Section、Lesson 不分别变成自治 agent。只有信息可见性变化、真实人类等待、值得保存的阶段成果和一致版本检查等边界由运行程序保证。教学行动由模型推理决定，几十个验证场景不变成教师菜单或逐场景生产节点。

工具至少支持范围受控的知识读取、实际产物读取与版本化写入、数学核验、必要图形／文档渲染、检查记录与真实待答。模型提交内容或提出动作建议；程序验证权限、状态、资源和版本。模型不能自行设置“人工同意”“检查通过”或交付事实。数学执行限制表达式、时间和资源；不把模型任意 shell、代码或 URL 直接交给服务执行。

### 4. 任务级调用约定

接口版本采用 `/v1`。调用方是受信 Web 后端；身份与授权来自服务认证上下文，不信任正文中自称的教师、管理员或租户角色。未启用能力、格式和不合法范围返回明确错误。传入外部材料应可解析为有权限的不可变内容快照，不能只保存一个会变化的“最新”链接。

| 操作 | HTTP 入口 | 最小请求语义 | 可观察行为 |
| --- | --- | --- | --- |
| 发起 | `POST /v1/tasks` | `event_id`、`capability`、自然语言 `instruction`、`scope`、`context_refs`、已有效的 `decisions`；可有明确资源限制 | 先保存请求并取得稳定 `task_id`，再异步执行；已有同事件返回原回执 |
| 查询 | `GET /v1/tasks/{task_id}` | 有权限的任务身份 | 返回持久的版本快照，不触发模型或续作 |
| 回应 | `POST /v1/tasks/{task_id}/replies` | `event_id`、`request_id`、`request_snapshot_id`、实际原始文字／选项／附件引用 | 原文保存后解释其教学意图；回执不表示已采纳或续作完成 |
| 修订 | `POST /v1/tasks/{task_id}/revisions` | `event_id`、`base_revision`、`target_refs`、自然语言 `instruction` | 明确修改依据及范围；运行中先阻止旧工作提交再串行处理；已结束任务创建有来源关联的新任务 |
| 续作 | `POST /v1/tasks/{task_id}/continuations` | `event_id`、停止对象与原因、确有变化的限制／依赖 | 从保存点继续未完任务；不能越过待答请求，也不能重开已取消任务 |
| 取消 | `POST /v1/tasks/{task_id}/cancellations` | `event_id`、可选原因 | 先禁止后续提交，再停止正在运行的工作；保留历史与成果 |

`capability` 区分 `curriculum_design`、`lesson_creation`、`lesson_adaptation`、`lesson_prep`、`check_for_understanding`。`scope` 明确数学学段、请求层级或焦点、内容范围与所需交付，CCSS 框架由服务固定。`context_refs` 指向实际课程、学校条件、学习证据等快照；具体个体字段按用途读取，不要求上传完整学生档案。

快照最少返回 `task_id`、`view_revision`、实际请求范围、运行状态、候选／已交付产物引用、当前待答、检查及未解决项、已采纳决定和累计消耗。运行状态区分排队、执行、待人、停止、失败、取消和完成；检查结论、人类参与及交付范围分别表达。完成只指该任务请求的工作及其能力退出条件成立，不等于课程已发布或学生已学会。

待答对象包含自然语言问题、必要的实际内容展示、目的、作用对象与版本、允许的回应形式。可提供少量有意义选项，同时接受自然语言；无需用户认识内部场景名。请求展示快照与产物版本分开，后台进度改变不自动使一个仍有效的回答失效。

变更操作返回持久事件回执，分别可查询接收、解释／采纳及续作状态。相同 `event_id` 在同一身份／客户端与操作范围内去重；相同身份事件却正文不同返回冲突。发起操作在尚无任务 ID 时也能去重。新的事件代表新的回应或意图，不能仅因旧请求已处理而丢弃。过期或冲突回应保留原文并解释适用范围，必要时请求澄清。

进度订阅只传受权限约束的业务信息，断流后重新查询权威快照；不向 Web 暴露原始模型消息或以 trace 当数据库。产物通过受控引用读取，校验访问范围及内容指纹。第一票交付发起／查询并发布由实际类型生成的 OpenAPI；其余操作随行为票加入，禁止空接口假实现。

服务外调用例和基本可信访问边界从首票开始，后续能力随行为更新；最后的五能力消费者票只做汇总与兼容核对。初始内容引用支持调用方已规范化的真实文本／结构内容和明确版本，不以任意上传格式解析作为备课前置，也不要求原课必须由本仓库生成。

### 5. 存储、调度与恢复

一个教学任务对应一个 Agent Server thread，可有多个 run；同一任务串行演进。业务存储保留任务、原始事件、展示快照、采纳决定、待续作动作、产物版本与检查引用；框架 checkpoint 保存执行续接状态，两者职责分明。

原始事件先持久接收。解释完成后，以条件更新将“动作采纳”与“待续作标记”在业务事务内一并保存，再交给 Server 调度。调度结果不明时先核对已存在 run／操作，再重试；执行前复查动作、取消和基线条件。模型调用可能因故障重复计费，但不得重复采纳人类决定、提交相同副作用或出现相互覆盖的有效版本。复用 Server 队列，不另造通用调度平台。

产物先写不可变内容并核验可读，再以引用提交版本；不能把数据库已提交但文件不存在的内容宣布交付。新检查绑定实际输入内容指纹和规则版本，旧检查不因文件同名而沿用。跨层修订保留修改前的依赖，并检查新产生的依赖，不能只遍历更新后的图。

Harness 提交本任务工作版本；跨任务共同课程基线由其所有者按预期旧版本进行条件采纳。外部未确认采纳不宣称公共基线已更新。个人支持变更保持自己的范围。

可恢复运行保存模型、规则、工具与状态语义版本。等待跨部署时保留兼容执行路径，或显式迁移并记录差异；仅写一个版本字段而没有可执行旧路径不合格。生产方向采用 Standalone Agent Server；冻结部署版本后核对许可、PostgreSQL／Redis、持久产物、备份与恢复。dev 重启证据不替代生产验收。

### 6. HITL 的教学语义

可自行检索的资料先检索，已有本次有效事实和决定直接使用。只有缺失且影响设计的真实条件、超出授权的重要取舍、能力要求的真实贡献或明确请求审阅才等待。等待跨越进程；定时器、模型回答或测试脚本不能冒充真实人类。

各能力遵守 Solution 中的独立流程：课程取舍不强加每层审批；创建不重复索取同一授权；适配整套接受与部分接受分开；备课先呈现原任务，正常路径在教师贡献前不泄露待其提出的 near-miss；CFU 明确改向后直接按新焦点构建，除非还有真实歧义。自然语言解释必须覆盖否定、条件、简短答复和改变意图，不依赖固定关键词选项路由。

教师的数学判断也可有误，应给出有据纠正；记录“参与者说过”不能将其提升为已验证数学事实。退出、取消、仅生成待后审和正常完成分别记录，不能用沉默补出满意或贡献。

### 7. 内容、检查与修订

共同任务使用同一实际内容源生成师生视图，按受众控制说明、答案和元数据。首批交付 Web 可展示、可打印的 HTML／SVG 与可编辑源；必要函数图必须实际绘制、检查和可读。Word 等显式要求仅在相应适配可用时承诺；不能默默改格式。独立的多轮精细制图平台后置，不影响当前必要图形的质量要求。

内容检查包括适用数学、解法、表征、学生推理、课堂条件、任务与目标、实际材料及跨层一致性。确定性计算、模型审阅、真实文件渲染和人工评阅分别保留证据与局限。程序检查不把教学判断写成大量硬编码情境；模型评分也不自授交付权。

CFU 在学生页与教师指南实际写入后执行两道检查：独立读者只收到学生可见内容，判断所有可辩护回答；另一路按实际数量检验每条错误／回应解释能否产生相应作答。单选、多选、开放题各按题型判断，不强制开放题“只有一个答案”。隔离读者不能接触答案、生成历史或缓存，也不能遗漏学生能看到的图题、描述和格式泄题。

问题返回生成模型核实与修订，保存具体对象、证据和变化；允许反驳检查误报。改动后重查受影响实际版本。必要范围未完、严重数学问题、关键学习机会缺失、必要材料坏损、伪造来源／人类或受众泄题阻止相应交付，不由总分抵消。停止保留草稿和未解决项，不降低要求宣布完成。

### 8. 观察、评价与消耗

从首条真实路径记录调用、工具、产物、检查、修订、实际 Context 清单及消耗，关联业务身份与版本。LangSmith 用于 trace、数据集与评阅，不充当人类决定、发布状态或主记录。真实服务连通先用独立合成数据验证，发送内容按身份和数据政策筛选；Key 不进入内容或日志。观察发送失败不使业务任务失败。

正常任务积累能力、范围、复杂度条件、质量、token、重试、工具、修订和耗时；活动与人类等待分开，跨恢复累计，未知值如实标记。保留失败和触限样本。先保障质量，再用同类任务比较优化；探索中的 12／24 次不成为默认上限。仍尊重明确资源限制、可配置防失控边界、取消及保存续作。

评价从能力实现开始，不等待五项齐全。上游比较固定版本与输入，报告原规则和本项目采用规则；优先同模型、可比工具／资料的机制比较，原宿主体验对照另报混杂因素。完整原创单元冻结后，独立评阅者匿名比较 A／参考 R，随后修订 B，保留全部版本。真实数学教育评阅校准和相关条件下的重复运行，是能力质量声明的前提；无教师伙伴时仍可完成实现与准备评阅包，但不得伪造人审或课堂结果。

评价执行与结果持久化可离线完成，不依赖 LangSmith 在线；云端观察与评价关联分别验收。提前开展的 Skills 案例不得读取目标 IM 材料或将参考输出带入原创 Context；需要目标 IM 才能完整复现的案例延后，不能删减参考要求。原创冻结、首轮实际意见和依据意见生成 B 是分别可核对的完成事实。

## Testing Decisions

以任务 API 的可观察行为为主要验收边界：提交真实输入，观察请求与回复、持久状态、实际产物及副作用。以同一入口替换可控模型／工具来注入故障，再用真实 Gemini 与实际 LC、渲染器验证贯通。仅在 API 难以定位的数学核验、知识语义和隔离边界增加小范围测试；不逐节点照抄实现写测试。

| 验证组 | 必须证明的行为 | 证据要求 |
| --- | --- | --- |
| 首条连通链 | 全年 CCSS 蓝图→真实连续课时→实际材料；真实模型与原生循环结合 | 输入与规则清单、实际知识调用、内容版本、渲染、检查及修订记录 |
| 教学范围 | 完整单元全部课时、目标—机会—证据；上层变化反馈下层；局部支持不改基线 | 覆盖清单及逐项内容引用，不能以目录和代表题代替 |
| 各能力 HITL | 各自正常参与与关键例外；已有授权、否定、条件、新意图和真实回应恢复 | 固定／合成行为案例与真实参与分别标注，加入未用于调 Prompt 的自然语言改写 |
| 知识与 Context | CCSS 范围完整，图方向与身份准确，模型读到实际 LC，输入隔离和容量处理有效 | 真实返回样本、缺页／空集／故障等反例、实际装配清单；不只断言工具已注册 |
| 材料与数学 | 实际题目可解、认知任务与焦点相符、师生一致、图形及输出可用 | 原创建 rubric、各能力规则、计算／独立读者、真实渲染与页面走查 |
| 事件与恢复 | 接收→采纳→调度各故障窗口、重送、取消、并发冲突和恢复不重复提交 | 硬退出与重启、实际持久数据、API 结果和副作用；不只检查 mock 调用次数 |
| 产品接入 | 身份隔离、材料读取、权限撤销、流重连、跨版本等待、备份恢复 | 测试调用方与生产候选部署的实际操作记录；负载和 SLA 以实际条件为据 |
| 比较与质量声明 | 能复现自身与上游差异，完整原创冻结后比较 IM，结论与证据范围一致 | 版本固定的案例、盲化材料、独立评阅及分歧；未评估和未复现明确保留 |

复用现有实验的失败与反例：坏 SVG／标签重叠、50 分钟课时矛盾、未展开内容伪装完整、无依据掌握度保证、实际未调用渲染器却自称渲染、检查误报；运行探针的重复／迟到事件及退出恢复转为新实现行为用例。保留原型作为先例，不复制其固定答案和测试控制接口到生产。

每张票验收“可运行的行为、当次实际质量、覆盖局限”三项。确定性用例通过不自动代表生成质量达标。完整单元和四项能力分别经对应评价；严重的新问题回到相关机制修正。用于广泛非劣结论的样本与界限在正式比较前根据试运行依据确定，不能看到结果再改门槛。

## Out of Scope

- 学校、教师、学生、家长的完整 Web UI、身份系统及学校工作流建设；Harness 的身份和交互对接仍在范围内。
- 学习证据采集和 Learner Model 构建更新；LC 数据下载、本地化或重建外部数据 API。
- 州标准选择、跨框架转换、首批实现其他学科和一次覆盖全部 K–12 数学的质量声明。
- 复制 IM 课程结构、原创冻结前读取对应 IM 材料、宣称消除了预训练知识的严格双盲。
- 自动发布、代替外部课程所有者采纳公共基线，以及凭离线内容结果保证学习效果。
- 前置建设通用制图平台、多 agent 层级树、自动模型路由和预算优化系统；必要图形、单模型接入、观察与运行保障仍需完成。
- 将任意文档格式、任意模型或核心任务变体视为首条切片已经支持；能力范围随实际实现和验证扩展。

## Further Notes

本规格中的传输命名、实现切片和测试组织是依据已定行为作出的实施选择，不伪称用户逐字段批准。用户已确认整体路线并要求持续推进；无需为例行拆票再开展一次访谈。若实施发现接口不足或存在更有效教学机制，应依据具体证据更新规格与相关决定；不得静默改变项目目标。

当前没有生产实现、完整单元通过、Skills 非劣结论或真实学校试用。本次仅完成从架构到实施的交接。既有研究及不成功实验继续保存，完整依据按 [研究索引的任务路由](../skills-harness/assets/index.md) 读取，以下为本规格的主要来源：

- 范围与运行：[能力约定](../skills-harness/issues/01-capability-contract.md)、[调用决定](../skills-harness/issues/02-execution-interface.md)、[执行结构](../skills-harness/issues/03-execution-structure.md)、[模型／工具](../skills-harness/issues/04-runtime-adapters.md)、[主执行设计](../skills-harness/assets/main-execution-design.md)、[知识与产物设计](../skills-harness/assets/model-knowledge-artifact-design.md)。
- 教学设计：[课程体系](../skills-harness/assets/curriculum-design-capability.md)、[Context](../skills-harness/assets/context-and-llm-design.md)、[课时创建](../skills-harness/assets/lesson-creation-execution-design.md)、[适配与备课](../skills-harness/assets/execution-contract-draft.md)、[CFU](../skills-harness/assets/cfu-execution-design.md)、[HITL](../skills-harness/assets/hitl-design.md)。
- 知识与评价：[知识使用约定](../../docs/references/knowledge-consumption-contract.md)、[教案全部 rubric 取用](../skills-harness/experiments/grade8-linear-functions/evaluation-and-adoption.md)、[质量与生产验证路线](../skills-harness/assets/validation-and-release-route.md)。

## Comments

### 2026-09-15：整体交付交接

将用户确认的整体功能路线固化为五项能力的实施规格。首条连续课时路径、完整单元、独立教学能力及产品验证分别交付；质量、HITL、恢复和观察贯穿其中。未将预算优化或独立图形模块设为前置。

### 2026-09-15：逐票规模与依赖复核

纠正原 14 票中大工作包、共享能力包装、观察与评价绑定及双重完成口径。依据 [复核记录](ticket-sizing-review.md) 拆成 22 个工作项，6 个远端工作包待实际输入后收口；最终功能与质量目标保留。基本权限、持久事务和每项能力自身检查不因拆票延后。
