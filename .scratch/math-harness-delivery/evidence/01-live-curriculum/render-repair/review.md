# 修复代码的两轴审查

基点：`0631f016a8080b1d1e6321ddeb4b19e4b6499980`。提交前对工作区执行 `git diff 0631f01 --`，由两个独立审查 Agent 按 `code-review` 分别检查。

## 规范轴

初查未发现硬性违规。追加格式防漏检查时发现数字、负号、空格开头及尾部多余美元符号仍能漏过；已增加先失败后通过的反例，修改为未匹配定界符须修复或显式转义，保留独立金额和代码语义。

规范轴独立复核 6 个定向探针通过，原发现已修复，剩余 0 项。

## 规格轴

初查发现两项：未闭合公式被当作普通正文输出；最终审阅刚跨过显式 token 阈值时，纯程序检查提交也被阻断。两项均以公共内容／编译图接口复现，先红后绿。前者返回字段级错误，后者把 token 阈值核对移到新增调用边界。规格轴复核无剩余阻断项。

两轴分别报告，不用测试通过替代规格核对。最终真实内容、API 检查、HTML 指纹及浏览器证据见[验证报告](README.md)。

最终验证：65 项测试通过（32.56 秒），Ruff、mypy 和 `git diff --check` 通过。汇总：规范轴 0 项，规格轴 0 项，无剩余最高等级问题。
