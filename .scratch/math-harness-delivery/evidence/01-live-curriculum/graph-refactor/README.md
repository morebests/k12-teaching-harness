# 阶段图重构验证（2026-09-16）

## 结论

已将单节点内部循环改为五个主阶段：`prepare_task → author → prepare_review → reviewer → record_review`，阻断检查经条件边回到作者，受控停止进入 `finish_incomplete`。作者与审阅使用静态 `create_agent` 子图，其余阶段为普通 Python。设计与边界见 [目标图](../../../ticket-01-graph-refactor-design.md)。

最终自动回归为 **51 项通过**，见 [pytest 输出](pytest.txt)；Ruff 和 mypy 通过，[两轴审查](review.md)均无剩余发现。真实 Agent Server 验证了维护者读取实际子图检查点，普通教学接口保持消息隔离。两轴审查发现的阅读稿身份路径、补查知识未进入送审、坏引用导致停止出口再次抛错三项均已修复，并用先失败后通过的用例复核。

**本轮真实 Gemini 样本最终为 `stopped`，不能记为教学质量通过。** 它实际走过生成、两次独立审阅、修订和资源停止；未成功排版的草稿没有被提升为已交付成果。首票此前的真实通过样本保持原来的证据范围，不被本次失败样本改写。

## 自动验证范围

- 原生 API 重送、并发、授权、普通与维护者流、断流、取消、失败及本地诊断文件权限。
- `get_subgraphs()` 枚举真实作者和审阅子图；通过原生 Server 的历史检查点展开内部消息，验证父图无共享消息，审阅不继承作者工具消息。
- 作者完成后从送审继续、审阅完成后仅提交检查；固定依据在重新构图后继续使用，不重查已不可用的知识服务。
- 检查文件早于父检查点落下时幂等提交；送审后内容改变或图件引用损坏不沿用旧检查，保留原因、累计用量和未完成结果。
- 阻断检查经条件边修订，每轮消息独立；未知模型消耗不重复调用，旧图记录不被归零；同轮并行工具共享额度。
- 同锁读取正文、实际 SVG／参数、阅读稿身份与补查知识；实际 HTML 指纹和补查依据进入送审输入。

恢复测试使用 LangGraph 公共图接口、内存 checkpointer 和可控外部依赖，服务读取测试使用真实 `langgraph dev`。这些不是生产崩溃恢复、公开续作 API 或教师 HITL 验收。

## 真实任务

| 项目 | 实际结果 |
| --- | --- |
| 任务 | `003b6069-9b22-50b6-8203-00c2e7bec417` |
| 原生 run | `01a0a765-cdfa-7890-b206-17e60345131e` |
| 模型 | `gemini-3.8-flash`，实际包版本见 [上下文记录](context.json) |
| LC | 本地 HTTP 服务，`lc/v1.11.0`；[查询审计](knowledge-audit.json) 保存真实查询、分页与返回指纹 |
| 输入 | 新的合成学校条件，3 课时、每课 50 分钟、28 人；目标 `8.F.B.4`，没有实际学生掌握证据 |
| 模型调用 | 30 次 |
| 工具调用 | 26 次；另有 2 次 `Review` 结构化输出，不算外部工具执行 |
| token | 输入 1,569,550，输出 85,802，总计 1,655,352；未知消耗为 false，费用未测 |
| 活动时间 | 293.84 秒 |
| 最终状态 | `stopped`：剩余 token 不足以安全预留下一次调用 |
| 当前材料 | `rendered=false`，检查不可适用于最终指纹，`passed=false` |

第一轮作者先读取当前稿，绘制雨水桶函数图，调用有理数计算器核对变化率、初始值和反求时间，保存草稿时遇到参数／指纹错误，随后读取并重新保存。审阅用计算器复核，并发现实际图的主刻度与题目要求、教师说明不一致。程序同时发现排版未完成，工作流返回作者。

第二轮作者调整图参数并修订。第二次模型审阅返回零项发现，但程序仍发现排版未完成，`record_review` 再次返回作者。第三次作者尝试修订时达到 token 预留边界，保留当前草稿后停止。这里明确区分了模型审阅结论、程序检查和最终任务状态。

[工具调用完整配对](tool-calls.json) 包含每次阶段、轮次、子图命名空间、调用 ID、开始／返回顺序、实际参数、返回与错误。28 组请求均有对应返回，其中 26 组是实际工具，2 组是 `Review`。例如首轮 `(145-85)/(7-3)` 得到 `15`、`85-15*3` 得到 `40`；第二轮 `(80-20)/5` 得到 `12`。不能把多种原生流里的重复事件当作额外调用。

最终 [API 结果](api-result.json)、[执行摘要](execution-summary.json)、[当前 JSON](content/curriculum.json) 和 [实际 SVG](assets/rainwater_tank.svg) 均保留；没有有效阅读稿，因此不生成一个占位 HTML 冒充成功排版。完整原生 JSONL 留在本机 `work/graph-refactor-live/trace.jsonl`，权限 0600，不进入 Git。

## 实测与最终修复的关系

真实任务运行期间，代码审查补出了送审阅读稿身份路径与补查知识交接两项问题。该 run 使用启动时的代码：它的 `render_identity` 为空，本次模型也没有额外调用 `browse`。两项后续修复由上述公共接口用例及最终完整回归覆盖；没有把这一次 run 宣称为所有最终修复的真实模型验证。停止出口坏引用修复也由故障注入回归覆盖。

验证前只读核对原 2024 开发服务没有活动任务；实测使用独立端口和工作目录，结束后关闭该临时服务，原服务保持原样。四个 LangSmith 新旧追踪开关均在本次进程中显式关闭，未上传本次任务或历史内容；此设置不改变项目尊重服务端显式追踪配置的行为。

## 保留的限制

本次没有修复真实草稿的排版问题，没有取得教师校准、课堂效果或浏览器视觉验收。节点阶段接续已有限验证，供应商请求、文件写入和 checkpoint 仍不是一个事务；未知动作保守停止。旧 `curriculum_work` 检查点不能直接迁移到新拓扑，生产恢复、取消迟到写入、公开续作和 LangSmith 平台关联仍按后续票完成。
